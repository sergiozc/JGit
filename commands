
echo "texto" >> nombreFichero       	  Para crear un archivo de texto con algo puesto (texto)
git status                          	  Para saber el estado del repositorio
git add (fichero)                   	  Para añadir un fichero a la staging area
git add .                           	  Para añadir todos los ficheros del worksapce a la staging area
git commit -m "Commit n1"           	  Para entregar el fichero que estaba en la staging area con un mensaje de commit.
git push "HTTP clone" (nombre_rama)       Para subir el repositorio a GitHub
git init (nombreRepositorio)        	  Para crear un nuevo repositorio vacío.
git clone "(URL)"
git branch (nombre_rama)                  Para crear una rama
git checkout (nombre_rama)                Para mover el puntero (HEAD) a una rama o a un commit
git checkout HEAD^                        Para colocarnos en el commit anterior
git checkout HEAD~4                       Para colocarnos 4 commits antes
git branch -f main HEAD~3                 Para mover la rama main 3 commits hacia atrás
git merge (nombre_rama)                   Para combinar dos ramas. Nos situamos en la rama de destino (donde se acopla la otra)
git rebase (nombre_rama)                  Para combinar ramas de forma + lineal.
git reset HEAD~1                          Para mover la rama (local) en la que estamos hacia atrás (eliminando el commit)
git revert HEAD                           Para revertir en una rama remota
git cherry-pick (C1, C2)                  Para copiar los commits especificados
git rebase -i (hasta donde)               Para copiar los commits hasta donde se indique (dando la posibilidad de ordenar)
git branch [-a]                           Para ver todas las ramas disponibles
git branch -d (branch name)               Para borrar rama local
git push origin :(nombre_rama)            Para borrar rama remota
git tag (nombre_tag)                      Para crear una etiqueta
